# JOURNAL — Helm

> Dated log of decisions, pivots, incidents, and quotes. Add entries as
> things happen — retrospectives need this raw material to land.
> Reverse-chronological; one paragraph max per entry.

## 2026-05-29 — Canonical URL is helm-bridge.vercel.app #decision

The auto-assigned `helm-jade.vercel.app` had no semantic tie to the project. Switched to `helm-bridge.vercel.app` — the bridge of a ship is where steering happens (Helm's metaphor), and "bridge" also lands the MCP angle (Helm bridges the LLM to four mock business systems via MCP servers). `helm-ops.vercel.app` was the first pick but already taken on Vercel's namespace. Removed the jade alias to keep a single canonical URL.

## 2026-05-29 — Dashboard live on Vercel at helm-bridge.vercel.app #milestone

First deploy landed. Scope: `sankofa-forge` (matching the other portfolio repos — clearhash, quarry, celestial-sanctum). Project name: `helm`. Vercel auto-assigned the alias `helm-jade.vercel.app`. Configuration was the tricky bit — pnpm-workspace + Vite SPA + a JSON import that crosses workspace boundaries needed an explicit `vercel.json` at the repo root with `buildCommand: pnpm --filter @helm/front build`, `outputDirectory: front/dist`, and `framework: null` so Vercel didn't try to auto-detect a wrong preset. First `vercel deploy --yes` attempt failed because Vercel inferred the project name from the directory path which contains parens and uppercase letters; fix was `vercel project add helm` → `vercel link --project helm` → `vercel deploy`. Build time on Vercel: ~19 s. The committed mock measurement under `data/measurements/output/seed-1/invoice-ocr/report.json` ships in the bundle, so the dashboard renders the same numbers locally and in prod. The MOCK DATA badge is honest in the URL footer; the next Groq run rewrites that to MEASURED.

## 2026-05-29 — Dashboard scaffolded; AP Invoice OCR panel renders live measurement #milestone

front/ workspace landed — React 19 + Vite 6 + Tailwind 4 (`@tailwindcss/vite`) + Chart.js. Single panel for now (AP Invoice OCR), matching the CLAUDE.md anti-slop rule "if a sub-feature isn't done, it's not in the dashboard." Other three sub-features show in the sidebar as `NOT YET SHIPPED` placeholders rather than being hidden — gives the reader the map of the contract without faking progress. Brand palette in `index.css` via Tailwind 4's `@theme` directive: deep-navy bg, libsql-teal accent, warn-orange for the mock banner. The dashboard reads `data/measurements/output/seed-1/invoice-ocr/report.json` directly via Vite's `server.fs.allow` (parent dir) — when the back/ Express API ships, this becomes a fetch against `/api/measurements/...` and the allow-list shrinks back. Components: TopBar (extractor badge auto-flips between MOCK DATA and MEASURED · Llama 4 Scout), Sidebar (4 sub-features, only AP active), four MetricCards on the main panel (parse rate, field acc, cost/invoice, labor-recovery ratio), LatencyHistogram (12-bin Chart.js bar chart), Reconciler card (precision/recall/F1 + 2×2 confusion grid), RecentActivity table (last 14 invoices with status pills). Production bundle: 442 KB JS (123 KB gzip), 15.6 KB CSS — small enough to stay snappy.

## 2026-05-29 — Repo brought up to the standard baseline #milestone

Filled the gaps against the global standard-repo checklist in one sweep: banner SVGs (dark + light, 1200×420) with a ship's-wheel mark in libsql teal, paired PNGs via `rsvg-convert`; favicon.svg + 180×180 apple-touch-icon (full-bleed on the deep-navy brand color); 1200×630 social-preview card. Test runner: vitest, with 28 unit tests across `rng.ts` (determinism + fork independence + bool() frequency), `orders/policy.ts` (per-tier rates, elite promo-credit exception, FX conversion, threshold edge cases), and `back/src/ap/reconcile.ts` (each anomaly type, cent-tolerance, multi-flag invoices). CI workflow at `.github/workflows/deploy.yml` gates merges on typecheck + unit tests + a 10-invoice mock OCR smoke run. Playwright configs for the QA + demo suites with `playwright-bdd@8.5.1` (7.5.0 was incompatible with Playwright 1.60's internal `configLoader` path). Dependabot weekly schedule, grouped by ecosystem so PR noise stays low. No GitHub remote or deploy step yet — those land when the dashboard is ready to be served.

## 2026-05-29 — Groq free-tier daily cap discovered; renderer tightened #incident #decision

First real Groq Llama 4 Scout measurement run surfaced two surprises. (1) The free tier's 30 RPM rate limit was easy to absorb with SDK `maxRetries`. (2) The free tier's **500 K-tokens-per-day cap** was not. Each invoice burned ~6,250 tokens — roughly 825 just for the 2048×2640 px PNG (DSR=2 amplifies image cost more than I appreciated) plus ~1,500 prompt + ~300 output, plus retry attempts that also count toward the budget. The first 200-invoice run cleared 80 invoices (94.5% field accuracy, 88.7% line-item recall — directionally real Groq numbers) before tripping the daily cap. Worse, the followup 20-invoice run was issued from the same daily window and overwrote the 80-invoice `report.json` with twenty quota-exhausted error records before I noticed the measurement script always overwrites the same output path. Lost the 80-invoice data. Lesson: when running real measurements with non-recoverable side effects (cost, quota burn), the script should write to a timestamped subdirectory, not a single canonical output dir. Filed as a parking-lot item; for now, restored the mock baseline from HEAD~. Tightened the PNG renderer to viewport 1024×1320 at DSR=1 (down from DSR=2) — produces ~210 image tokens per invoice instead of ~825 (~4× reduction). A future 200-invoice run should fit in roughly 400 K daily tokens with comfortable headroom.

## 2026-05-29 — Pivot: Supabase → libsql (SQLite + vector) #pivot #decision

User caught Supabase in `.env.example` and questioned whether it was actually in the stack. It wasn't — those env vars were aspirational. Locked libsql in instead via `@libsql/client`. Rationale: data volume is tiny (a few thousand rows total, ten KB articles to embed), libsql ships native vector support (`F32_BLOB` + `vector_distance_cos`) so no separate pgvector setup is needed, and the dev story is a single file at `data/helm.db` with zero account/Docker overhead. Deploy path stays open — Turso Cloud has a free tier and the SQL is identical between local file and remote URL. Hiring-manager-readable story is *better* with libsql than Supabase here: "I picked the right tool for 300 rows of vector data" reads as engineering judgement, not pulling-in-a-managed-service-because-it's-cool. Same provider-agnostic principle as the Llama pivot — adopting libsql doesn't lock out a future Supabase swap if a real-deployment context needs it.

## 2026-05-29 — Pivot: Claude → Llama 4 Scout via Groq #pivot #decision

Swapped the locked LLM from Anthropic Claude to Llama 4 Scout served via Groq. Reason: cost. Claude Sonnet 4.6 vision is ~$0.01/invoice; Llama 4 Scout via Groq is ~$0.0004/invoice (~28× cheaper) and the Groq free tier covers development. User explicitly does not want to pay for Anthropic API. Flagged the resume tradeoff before pivoting — FHI Heat and Source Creative job postings name Claude verbatim, so removing it from the stack does cost some surface-level keyword match. Mitigation: the `Extractor` interface in `back/src/ap/extraction.ts` is provider-agnostic — a Claude implementation can sit alongside the Groq one if a specific application benefits, with no measurement-script changes. Side effect of the pivot: PDF rendering is replaced by PNG rendering (Groq vision API accepts images, not PDFs); the multi-page-layout anomaly now manifests as a tall single PNG via `fullPage: true` rather than a literal 2-page PDF. Renamed `data:render-pdf` → `data:render-png`. The mock extractor's pricing constants now reflect Llama 4 Scout's announcement-time public rates so the cost-tracking numbers from `--extractor=mock` are no longer Claude-flavored. CLAUDE.md, README.md, docs/scope.md, CONTRIBUTING.md, .env.example all updated to reflect Llama as the locked model.

## 2026-05-28 — AP invoice OCR pipeline scaffolded with mock extractor #milestone #decision

Sub-feature 1 reaches end-to-end-runnable in mock mode. Pipeline: PDF render (Playwright) → mock vision extraction (label + controlled noise) → Zod validation → reconciliation (math + anomaly flags) → measurement script with per-invoice traces + a headline block. Measurement output for seed=1 lives under `data/measurements/output/seed-1/invoice-ocr/`. Architectural choice worth flagging: the extractor is an `Extractor = (pdfPath) → Promise<ExtractionResult>` interface with two implementations (`createMockExtractor` ships today, `claudeExtract` will plug in later) so swapping in real Claude is a one-line change in the measurement script's `buildExtractor`. **The summary.md and headline numbers carry a "mock extractor" warning banner when running with the mock so the README can't accidentally quote simulated numbers as real ones.** This matters because the README headline finding is a contract per [`CLAUDE.md`](CLAUDE.md). User explicitly chose mock-first to validate the pipeline scaffolding without spending API budget — the swap to real Claude is the gating event for the headline finding.

## 2026-05-28 — Synthetic-data generators landed (seed=1 committed) #milestone #decision

Three generators built and seed=1 fixture committed (commit `aa5e9d5`). Architectural call worth flagging: the creator-payout policy lives in **two** files — `orders/policy.md` (the natural-language version Claude reads) and `orders/policy.ts` (the programmatic mirror that produces ground-truth payouts for measurement). They must move together; the deterministic re-computer in policy.ts IS the ground truth Claude is scored against. Caught one generator bug mid-verification: the duplicate-invoice-number anomaly was collapsing all six duplicates onto the same source number (one invoice number appearing seven times). Fixed by picking randomly from the issued-list rather than always reusing the first issue. Lesson: verify fixture distribution, not just fixture counts — the count looked right but the underlying structure was wrong.

## 2026-05-28 — Project kickoff #milestone #decision

Helm claims AI_ML slot 1 of 3. Scoped against five Handshake postings (Smart Circle, FHI Heat, Source Creative, BIOMED, Equitar); Helm directly targets the first three, which all describe the same SMB-back-office shape. Locked the four sub-features as the contract (AP invoice OCR, creator payout reconciler, Tier-1 customer service, cross-company KPI Q&A). Lane committed: agent/automation, not applied ML — that's slot 2's job (GLP-1 outcomes predictor). Stack locked to Node + Express + React + Chart.js + Anthropic SDK + MCP, matching the FHI Heat posting verbatim. Repo named *Helm* for the ship-steering metaphor and to match the user's existing single-word naming style (Halberd, Quarry, EconOS).

## 2026-05-28 — Headline finding is a contract, not a sketch #decision

Wrote into `CLAUDE.md` that the README banner finding must be backed by a re-runnable measurement script under `data/measurements/`. This is the anti-AI-slop guardrail that matters most: portfolio repos in this space tend to claim outcomes their code can't reproduce, and the user's portfolio bar (CapitolAlpha's "+2.58% Jensen's alpha, p < 0.05") is set higher than that. Every README number traces to a measurement file or it is not allowed in the README.
